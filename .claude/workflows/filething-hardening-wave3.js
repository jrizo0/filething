export const meta = {
  name: 'filething-hardening-wave3',
  description: 'Wave 3: fix apps/cli (commands, signed_vault, main/env/config/credentials, service/logrotate/progress/errors)',
  phases: [{ title: 'Fix', detail: 'one agent per disjoint file set inside apps/cli' }],
}

const REPO = '/Users/jrizo/filething'

// Handoff notes gathered from waves 1 and 2, so wave-3 agents know what changed
// underneath them. Extra notes can be appended at launch time via `args`.
const HANDOFF = [
  '1. `ft_core::CanonicalPath::validate_untrusted()` / `ft_core::FileEntry::validate_untrusted()` are the inbound',
  '   trust boundary for anything decoded from a Manifest page (rejects `..`, absolute, empty components, NUL,',
  '   backslash, drive prefixes, and unsafe symlink targets) -> `ft_core::Error::UnsafePath { path, reason }`.',
  '2. `ft_manifest::decode_page_verified(obj, &cid)` verifies a page hashes to its referring cid ->',
  '   `ManifestError::PageCidMismatch`.',
  '3. `EngineError` gained `Refused(String)` (a safety guard declined a destructive operation) and',
  '   `SpaceLocked { root, holder }` (another process holds the Space lock).',
  '4. ft-diff hardened the whole read path. New `ft_diff::Error` variants: `UnsafeEntry`, `OutsideSpaceRoot`,',
  '   `SymlinkedParent`, `ContentMismatch`, `PageDepthExceeded`. `ft_diff::TMP_SUFFIX` (".ft-tmp") is now public.',
  '5. ft-index: WAL + busy_timeout + synchronous=NORMAL; stamps `PRAGMA user_version` and returns',
  '   `ft_index::Error::SchemaTooNew` when a NEWER binary wrote the DB (its message tells the user to run',
  '   `filething update` — do not let a `.context()` wrapper bury that sentence); DB file chmod 0600; new batch',
  '   writers `upsert_entries` / `put_blocks`.',
  '6. ft-watcher: additive health/re-arm API — `WatchHealth`, `Watcher::is_healthy()`, `rewatch()`,',
  '   `root_is_intact()`, `health()`.',
  '7. ft-conflict `merge3` now returns `Conflict` (instead of silently fusing wrongly) when two sides delete',
  '   different copies inside a run of identical lines. Signature unchanged.',
  '8. ft-vault: `S3Vault` error messages now carry the real cause + HTTP status + S3 code (e.g.',
  '   "dispatch failure: io error: dns error: ... (http 403, code AccessDenied)"); `FsVault` validates keys and',
  '   writes atomically.',
  '9. The release/dist profiles now enable `overflow-checks`, and `strip` was relaxed so panic backtraces are',
  '   usable. Arithmetic that silently WRAPPED in a release build will now panic.',
  '10. Convex backend changes that affect this crate (see the per-agent assignments for the required client work):',
  '    - `vault:sign` now signs CREATE-ONLY PUTs (`If-None-Match: *` is a SIGNED header).',
  '    - `revisions:listFromSeq` now THROWS `too_many_revisions` past 4096 revisions instead of silently',
  '      truncating, and accepts a new optional inclusive `maxSeq` arg for windowed paging.',
  '    - `revisions:commit` rejects a non-32-byte `manifestRootCid` (`bad_manifest_root_cid`); `spaces:create`',
  '      rejects a non-32-byte `metaBlobCid` (`bad_meta_blob_cid`).',
  '    - Better Auth now rate-limits `/sign-in/email` (5/60s) and `/sign-up/email` (5/3600s), returning 429.',
  '    - None of the new error codes contain the substring "conflict", because',
  '      `crates/ft-coordinator/src/lib.rs` treats any message containing it as a commit CAS conflict. PRESERVE',
  '      that property.',
  (typeof args === 'string' && args.length > 0) ? '\nADDITIONAL NOTES:\n' + args : '',
].join('\n')

const SHARED = [
  'You are fixing production-readiness defects in the "filething" codebase at ' + REPO + '.',
  '',
  'WHAT IT IS: a Rust CLI (`filething`) that syncs a folder tree ("Space") across a user\'s own machines with',
  'client-side encryption. Data plane = S3-compatible Vault (R2/MinIO) of content-addressed encrypted Blocks,',
  'reached via 15-min presigned URLs minted by the Coordinator. Control plane = Convex ("Coordinator") holding',
  'the Space head, the Revision chain and manifest pointers. Spec: docs/format.md. Decisions: docs/adr/*.md.',
  'Ubiquitous language: CONTEXT.md. Promised behavior: README.md.',
  '',
  'You are working in apps/cli — the binary. It ORCHESTRATES ft-engine; no sync logic lives here. This is the',
  'entire surface a real user touches, so error text, defaults, confirmations and exit codes are the product.',
  '',
  'The defects below were found by an audit and each SURVIVED an adversarial refutation pass, so treat them as',
  'real. But you own the fix: if the diagnosis is wrong or the suggested fix is unsound, do the right thing and',
  'say so in your report.',
  '',
  '=== WHAT ALREADY CHANGED IN THE CRATES BELOW YOU (waves 1 and 2, already merged) ===',
  HANDOFF,
  '',
  '=== HOUSE RULES (this codebase is unusually disciplined; match it) ===',
  '- Comments explain WHY, not what. Cite the spec section or ADR when a rule comes from one. Match the',
  '  surrounding comment density and tone exactly. Several comments here cite GitHub issue numbers',
  '  (e.g. "issue #16") — keep that convention when you touch adjacent code, but do not invent issue numbers.',
  '- USER-FACING TEXT IS IN ENGLISH. Note apps/cli/src/errors.rs currently has SPANISH messages in an otherwise',
  '  English CLI — that is one of the defects. Do not add new Spanish strings.',
  '- Every behavior change needs a test that FAILS before your fix and passes after. Match the existing test',
  '  naming style (long, sentence-like snake_case names) and put tests in the file\'s existing `mod tests`.',
  '  There is also apps/cli/tests/cli_flow.rs (an integration test, gated on live backend env vars) — you may',
  '  extend it if your fix belongs there.',
  '- Do NOT change a function signature another file in this crate calls unless your instructions say to. Other',
  '  agents are editing sibling files in this same crate RIGHT NOW.',
  '- Do NOT edit any file outside the FILES YOU OWN list.',
  '- Keep changes surgical. Do not reformat, rename, or refactor code unrelated to your fix.',
  '- Adding a dependency: only if your instructions name one, and prefer something already in the workspace',
  '  lockfile. Check Cargo.lock before proposing anything new.',
  '',
  '=== DEFINITION OF DONE (you MUST run these and they MUST pass) ===',
  '  cargo fmt -p filething',
  '  cargo clippy -p filething --all-targets -- -D warnings',
  '  cargo test -p filething',
  'The whole crate compiles together, so a sibling agent\'s in-progress edit can make the build red for reasons',
  'that are not yours. If you hit an error in a file you do NOT own, do not fix it and do not give up: re-run',
  'after a short wait, and if it persists report exactly which foreign file and error blocked you. Never report',
  'success on a red build.',
  '',
  'Report back: (a) each defect and what you changed, (b) tests added and what they prove, (c) command output',
  'proving fmt/clippy/test state, (d) what you deliberately did not do and why, (e) handoff notes.',
].join('\n')

const GROUPS = [
  {
    key: 'commands',
    prompt: [
      'FILES YOU OWN: apps/cli/src/commands.rs',
      '',
      'DEFECT 1 (HIGH, password shown on screen). `read_password` (~line 117-136) reads the account password with',
      'a plain `stdin().read_line()` and NO terminal echo suppression, so it is displayed as typed and stays in',
      'scrollback / `script` / tmux capture logs. That password gates the server-escrowed `spaceKey` and',
      '`dedupSecret`, i.e. it is the encryption boundary, not merely a login.',
      'FIX: read with echo disabled when stdin is a TTY. Use the `rpassword` crate if it is already in Cargo.lock;',
      'check first, and if it is not there, add it (this is an explicitly sanctioned dependency — it is small and',
      'the standard choice) or set/restore termios ECHO manually. Keep the `$FILETHING_PASSWORD` path and the',
      'non-TTY fallback (piped stdin, CI) working exactly as they do now, and keep the doc comment honest about',
      'which path does what.',
      '',
      'DEFECT 2 (HIGH, silent absorption of an unrelated folder). `clone` (~line 332) only refuses when the target',
      'already contains a `.filething/index.db`; a NON-EMPTY target is accepted silently. The first pull then',
      'reconciles against an EMPTY base, so every pre-existing file under the target is absorbed into the Space',
      'and — because the auto-installed daemon runs a startup commit — uploaded and replicated to every other',
      'device of the account. Colliding paths are renamed to conflict copies with the remote winning the real',
      'path. The command prints no warning and no conflict list. `filething clone sp_abc ~/Documents` (a plausible',
      'typo for `~/Documents/notes`) does all of that to the user\'s whole Documents folder.',
      'FIX: refuse a non-empty target unless an explicit `--force` flag is passed (add the flag; you do not own',
      'main.rs, so report the clap wiring as a handoff note — or check whether `main.rs`\'s `Command::Clone`',
      'already has somewhere to put it and note precisely what the other agent must add). ALSO print the pull\'s',
      'conflict list instead of discarding it, so absorption is never silent.',
      '',
      'DEFECT 3 (MEDIUM, two machines collapse into one Device). `default_device_name` (~line 27) reads `$HOSTNAME`',
      'and falls back to the constant `"filething-device"` — but `HOSTNAME` is a shell variable that is NOT',
      'exported on macOS or in most non-interactive/systemd/launchd contexts, so in practice every device is named',
      '`filething-device`. Since `auth:ensureDevice` keys Devices by name, TWO machines silently merge into ONE',
      'Device record; the retention floor (`min(baseSeqInUse)`) then reflects the wrong device, and conflict copies',
      '(which embed the device name) collide.',
      'FIX: get the real hostname (check Cargo.lock for something already present — e.g. `hostname`, `gethostname`,',
      'or `sysinfo`; `libc::gethostname` via an existing dep is fine too). If nothing suitable is available, read',
      '`/etc/hostname` / run `hostname` as a fallback, and as a LAST resort make the generated name unique per',
      'machine rather than a shared constant. Report which you used.',
      '',
      'DEFECT 4 (MEDIUM, destructive command with no confirmation). `gc` (~line 910-960) runs an account-wide',
      'sweep that DELETEs Vault objects. `--apply` deletes immediately with no confirmation and no summary shown',
      'first, and the report is printed only AFTER the deletes. Separately, when `S3_*` is absent it exits 0',
      'having done nothing at all, so a user believes a GC ran when none did.',
      'FIX: (a) with `--apply` on a TTY, show what WOULD be deleted and require an explicit confirmation (with a',
      'flag to skip it for scripts — `--yes`; same handoff caveat about main.rs as DEFECT 2); (b) when the GC is',
      'not possible (no direct S3 credentials), FAIL with a non-zero exit and an explanation, never exit 0',
      'silently. Note another agent has added safety guards inside `ft_engine::gc` (a refusal when the account owns',
      'no Spaces, an ownership check, and a proportion cap exposed on `GcOptions`) — read the current `GcOptions`',
      'and wire the new field(s) through, and make sure a REFUSAL renders as a clear message rather than looking',
      'like a successful empty sweep.',
      '',
      'DEFECT 5 (MEDIUM, nested Spaces corrupt each other). `init`/`clone` (~line 251, 332) refuse only when the',
      'target itself is already a Space; nothing stops creating a Space INSIDE an already-mapped Space, where two',
      'engines then fight over the same files.',
      'FIX: walk up from the target and refuse when any ancestor is a mapped Space root, and refuse when the target',
      'CONTAINS a mapped Space root beneath it. Explain the conflict in the error.',
      '',
      'DEFECT 6 (MEDIUM, the CLI claims something false). `ensure_background_daemon` (~line 1226) prints',
      '"daemon: running in background" without checking the service actually loaded, so a failed launchd/systemd',
      'install still reports success and the user believes sync is running when it is not.',
      'FIX: verify the service state after installing and report honestly, including what to run to diagnose it.',
      '',
      'DEFECT 7 (MEDIUM, silent account rebind). `login` (~line 86) overwrites the stored identity with no warning',
      'when the machine is already logged in as a DIFFERENT account, orphaning the existing Space mappings (whose',
      'Spaces the new account does not own) — every mapped Space then fails with "not found or no access".',
      'FIX: detect the change and require confirmation, explaining what will happen to the existing mappings.',
      '',
      'DEFECT 8 (LOW, `status` is over-confident). `status` (~line 404-504) can report a perfectly healthy Space',
      'when NOTHING is syncing it (no daemon installed/running), and no command reports a Space\'s encryption',
      'state. FIX: have `status` report whether a daemon is actually watching this Space, and show the encryption',
      'state (`alg=1` vs a legacy cleartext Space) — `init` already prints it at creation, so the information',
      'exists.',
      '',
      'DEFECT 9 (LOW, ignored env var). `require_identity` (~line 140-154) silently ignores `CONVEX_URL` once a',
      'config exists, while its doc comment claims a verification it does not do. FIX: make behavior and doc agree',
      '— either honor an explicitly-set `CONVEX_URL` (warning when it differs from the stored one) or state plainly',
      'that the stored URL wins after login. Pick the less surprising one and justify it.',
      '',
      'Also: another agent may have added a per-Space advisory lock in `ft_engine::SpaceContext::open`',
      '(`EngineError::SpaceLocked`). If so, make sure the CLI renders that as a helpful message rather than a raw',
      'error — coordinate via apps/cli/src/errors.rs, which you do NOT own: report it as a handoff note.',
    ].join('\n'),
  },
  {
    key: 'signed-vault',
    prompt: [
      'FILES YOU OWN: apps/cli/src/signed_vault.rs',
      '',
      'DEFECT 0 (BREAKING — DO THIS FIRST, everything else in this file is moot without it). The Convex backend',
      'has ALREADY been changed to mint CREATE-ONLY presigned PUTs: `vault:sign` now builds',
      '`PutObjectCommand({ ..., IfNoneMatch: "*" })`. The AWS signer keeps `if-none-match` as a SIGNED header',
      '(it is not hoisted to the query string and not in ALWAYS_UNSIGNABLE_HEADERS), so it appears in',
      '`X-Amz-SignedHeaders=host;if-none-match`. **A client that does not send that header now gets 403',
      'SignatureDoesNotMatch on EVERY upload** — i.e. all commits fail.',
      'FIX in `put`:',
      '  - send `If-None-Match: *` on every presigned PUT;',
      '  - treat HTTP 412 PRECONDITION FAILED as SUCCESS, not failure: the key is content-addressed, so an object',
      '    that already exists necessarily holds the same bytes. This is the normal dedup case and must not error.',
      'This is safe against the current server too (S3/R2 honour the precondition either way), so it can land',
      'independently. Note `ft_vault::S3Vault` (the operator/GC path with direct S3_* credentials) does NOT go',
      'through `vault:sign` and needs no change. Add a test that pins both behaviours.',
      '',
      'This is the DEFAULT data-plane backend for end users: it fetches presigned URLs from the Coordinator and',
      'does the block/manifest HEAD/GET/PUTs itself. The operator-only `S3Vault` gets the AWS SDK\'s retries for',
      'free, so the path real users are on is currently the LESS resilient of the two.',
      '',
      'DEFECT 1 (HIGH, a stalled transfer wedges every Space). Every request runs on a bare',
      '`reqwest::Client::new()` (~line 104): no request timeout, no read timeout, no connect timeout. An',
      'application-level stall (a proxy or R2 that keeps the TCP connection alive, answers keepalives, but stops',
      'sending the body after headers) never returns and never errors. Because the daemon awaits the pull inline',
      'inside its `select!`, and ALL Spaces share ONE task, that one dead request freezes every Space: no backstop',
      'pull, no debounce commit, no watchdog, and `filething daemon` stops answering Ctrl-C. The log shows nothing.',
      'The same untimed client serves the commit path\'s PUTs.',
      'FIX: build the client with bounded timeouts, mirroring what apps/cli/src/auth.rs already does (~line 72-83):',
      'a tight `connect_timeout` plus a per-request bound. Prefer `read_timeout` (idle-based) over a total',
      '`timeout` for object bodies so a legitimately large block PUT on a slow link is not killed by elapsed time —',
      'reason about which applies to which verb and justify the numbers you pick in a comment.',
      '',
      'DEFECT 2 (HIGH, one transient 503 kills a whole sync). Each HEAD/GET/PUT is issued exactly once (~line 363)',
      'and any non-2xx becomes a hard error. There is no retry, no backoff, and no handling of the retryable',
      'classes (408, 429, 500, 502, 503 `SlowDown`, connection reset) — and R2 returns 503 under normal burst load.',
      'Since commit/pull fan these out with `try_collect`, the FIRST transient failure among thousands of objects',
      'aborts the entire operation. Worse, on a first commit the index rows are only written after the whole batch',
      'succeeds, so a commit that dies on object 3,900 of 3,946 re-PUTs every block from scratch — a large `init`',
      'over a lossy link may never converge.',
      'FIX: wrap the three HTTP calls in a bounded retry with JITTERED exponential backoff (e.g. 4 attempts, ~200ms',
      'base). Retry only transport errors and 408/429/5xx. Keep 404 non-retryable so `head`/`get` absence',
      'semantics do not change. On a 403, the presigned signature has most likely EXPIRED (15-minute TTL, and a big',
      'sync outlives it) — invalidate the cached URL and re-sign before retrying, rather than failing.',
      '',
      'DEFECT 3 (MEDIUM, credentials in the log). The presigned URL is logged (~line 322) including its',
      '`X-Amz-Credential` and `X-Amz-Signature` query parameters, so daemon.log accumulates working, time-limited',
      'write capabilities to the bucket — and users paste logs into issues.',
      'FIX: redact the query string (or log only the object key and the verb) everywhere a URL reaches a log or an',
      'error message. Check the error paths too, not just the happy-path tracing.',
      '',
      'DEFECT 4 (MEDIUM, unbounded cache that also goes stale). The presigned-URL cache (~line 94, 119) never',
      'evicts, so it grows with every distinct object for the daemon\'s lifetime; and it is never re-warmed after',
      'the 15-minute TTL expires, so long operations use dead URLs.',
      'FIX: bound the cache and make entries expire with (a little before) the URL TTL, so an expired entry is',
      're-signed rather than reused. DEFECT 2\'s 403 handling is the safety net; this is the primary fix.',
      '',
      'DEFECT 5 (LOW, signature forwarded off-host). The client follows redirects (~line 366), which would forward',
      'the presigned signature (and a PUT body) to whatever host a redirect names.',
      'FIX: disable redirect following for the data plane; a presigned S3/R2 URL should never legitimately redirect',
      'cross-host.',
      '',
      'DEFECT 6 (HIGH, uncapped download). `resp.bytes()` loads a whole response body into memory with no cap',
      '(~line 354), and the object size is remote-controlled — an attacker (or corruption) that puts a 2 GiB object',
      'at a block key OOMs the client. Note the caller now verifies the reassembled file against the manifest\'s',
      '`pcid`/`sz`, but that check happens AFTER the bytes are in RAM.',
      'FIX: enforce a maximum object size before/while reading the body (the legitimate maximum block size is',
      'defined in ft-core — read it and derive the cap rather than hardcoding a number), using',
      '`Content-Length` when present and a streaming/limited read so a lying `Content-Length` cannot beat it.',
    ].join('\n'),
  },
  {
    key: 'main-env',
    prompt: [
      'FILES YOU OWN: apps/cli/src/main.rs, apps/cli/src/env.rs, apps/cli/src/config.rs,',
      'apps/cli/src/credentials.rs',
      '',
      'DEFECT 1 (HIGH, the CLI tells users to leak their own keys). `env_filter` (main.rs:~274-284) passes',
      '`RUST_LOG` through VERBATIM as a GLOBAL filter, so `RUST_LOG=debug` also enables debug for the `convex`',
      'crate, whose websocket writer logs every outgoing message. Those messages carry the Account escrow',
      '`dedupSecret` (auth:ensureDevice), the per-Space `spaceKey` (spaces:create) and the Convex JWT in',
      'CLEARTEXT — and apps/cli/src/errors.rs explicitly tells the user to re-run with `RUST_LOG=debug` to get',
      'detail. A user follows that hint, redirects to a file, and pastes it into a GitHub issue, publishing both',
      'halves of their Space key material. If the debug run precedes an auto-daemon install, `RUST_LOG=debug` is',
      'captured into service.env and the daemon keeps writing fresh JWTs to daemon.log indefinitely.',
      'FIX: never hand `RUST_LOG` to the whole dependency tree. Compose the filter so third-party targets are',
      'capped (a hard `convex=info,tungstenite=info,reqwest=info` floor on top of whatever the user asked for),',
      'while the project\'s own `filething`/`ft_*` targets still honor the request. Consider a dedicated',
      '`FILETHING_LOG` that only raises this project\'s targets, and drop `RUST_LOG` from the captured-env list so',
      'it is not made permanent for the daemon (that list is in apps/cli/src/service.rs, which you do NOT own —',
      'report it as a handoff note). Also open the daemon log file 0600. The errors.rs hint change is also a',
      'handoff note (not your file): it should suggest `-v`, which is `info` and does not leak.',
      '',
      'DEFECT 2 (HIGH, every command hangs forever when the Coordinator is unreachable). `ConvexClient::new`',
      'returns Ok before any socket is established and nothing except `status` bounds the work with a timeout',
      '(env.rs:~141), so with the Coordinator down or WebSocket upgrades blocked (hotel/corporate wifi, captive',
      'portal, proxy), `login`/`init`/`clone`/`sync`/`spaces`/`gc` NEVER return — the only output is the convex',
      'crate\'s raw internal ERROR line repeated every ~15s.',
      'FIX: bound the connect + first call with a `tokio::time::timeout` (~20-30s) and map elapsed to ONE',
      'actionable message naming the URL ("could not reach the Coordinator at <url> — check your network, or',
      'CONVEX_URL"). Put the bound where every command benefits (the shared connect helper in env.rs) rather than',
      'per-command. Also silence the raw worker spam for one-shot commands via the default filter.',
      '',
      'DEFECT 3 (HIGH, fd and task leak that eventually breaks a healthy daemon). `spawn_jwt_refresh`',
      '(env.rs:~211) moves a `ConvexClient` CLONE into a `loop { sleep; set_auth_callback }` task with no shutdown',
      'path. `ConvexClient` only aborts its websocket worker when the LAST clone drops, so every `connect_authed`',
      'permanently leaks one websocket, one task and a token mint every ~12 minutes. The daemon reconnects on',
      'every quarantine retry of a structurally wedged Space (backoff caps at 300s, so ~288 attempts/day), so',
      'after a day it holds hundreds of open websockets — on macOS, where launchd services commonly get a 256 fd',
      'soft limit, EVERY operation then fails with EMFILE, including on the healthy Spaces.',
      'FIX: tie the refresh task\'s lifetime to the connection\'s. Return the `JoinHandle` (or a',
      '`oneshot`/`watch` shutdown signal) from `connect_authed`, own it alongside the client, and abort it when the',
      'client is dropped — e.g. a small wrapper struct whose `Drop` aborts the handle. Make sure the one-shot',
      'commands still work (they exit before the first tick, so they must not now block on a join).',
      '',
      'DEFECT 4 (MEDIUM, clock skew reintroduces the auth storm). The refresh timer (env.rs:~215) computes its',
      'sleep as `(exp - margin) - now` mixing a SERVER-issued absolute expiry with the LOCAL wall clock, so on a',
      'device whose clock is behind, the timer sleeps past the token\'s real expiry — reintroducing exactly the',
      'reactive reconnect storm the timer exists to prevent. FIX: derive the sleep from a monotonic duration and a',
      'conservative lower bound rather than trusting the local clock\'s agreement with the server\'s, and clamp it',
      'to a sane minimum/maximum.',
      '',
      'DEFECT 5 (MEDIUM, torn config bricks every Space). `Config::save` (config.rs:~129) writes `config.json`',
      'NON-ATOMICALLY and with no locking, so a crash mid-write (or two processes saving at once — the daemon',
      'restarts itself after `init`/`clone`/`unmap`) leaves a truncated file. That file holds the identity AND',
      'every Space mapping, so a torn write bricks every Space on the device.',
      'FIX: write to a temp file in the same directory, fsync, then atomically rename. Consider what should happen',
      'when the existing file fails to parse: a clear, actionable error naming the file beats a panic or a silent',
      'reset to defaults (which would drop every mapping).',
      '',
      'DEFECT 6 (MEDIUM, secrets sitting where they will be committed). The Space key and the local index live in',
      '`<root>/.filething/` INSIDE the user\'s synced folder (credentials.rs:~112) — which for a developer folder',
      'is very often a git repository. Nothing stops `git add -A` from committing the Space key.',
      'FIX: write a `.gitignore` (containing `*`) into `<root>/.filething/` when the directory is created, so the',
      'directory ignores itself regardless of the repo\'s own rules. Check the permissions story too: the config',
      'dir holding `credentials.json` is created with the default 0755 unless something tightens it',
      '(credentials.rs:~134) — the file is 0600, but make the DIRECTORY 0700 on creation as well.',
      '',
      'DEFECT 7 (MEDIUM, stale env silently hijacks the data plane). `env.rs` (~line 359) lets leftover `S3_*`',
      'variables take precedence over the presigned path, so a shell that still has an old bucket exported writes',
      'the account\'s blocks into the WRONG bucket — the commit succeeds, the head advances, and the blocks are',
      'orphaned where no other device can read them.',
      'FIX: make the choice of data plane explicit and visible. At minimum log which backend and which bucket is',
      'in use at the start of any command that transfers data, and detect the mismatch case: a Space whose blocks',
      'were previously written elsewhere should not silently start writing to a different bucket. Think about what',
      'is actually detectable from here and implement that; report what is not.',
      '',
      'DEFECT 8 (LOW, exit codes and non-TTY output). Every failure exits 1 (main.rs:~258), so a caller cannot',
      'distinguish "not logged in" from "network down" from "conflict". And long transfers print NOTHING when',
      'stderr is not a TTY (main.rs:~343), so CI logs and `filething sync > log 2>&1` show silence for minutes.',
      'FIX: introduce a small, documented set of exit codes (keep 1 as the generic failure so nothing regresses),',
      'and emit periodic plain-text progress lines when not a TTY instead of the rewriting spinner. Do not make',
      'the TTY path noisier.',
      '',
      'DEFECT 9 (LOW, help text that lies). `sync --help` (main.rs:~124) says it "does not run the daemon" while',
      'the command INSTALLS and starts an OS service by default. FIX the text for `sync` and check the other',
      'subcommand docs in the same file against what the code actually does.',
      '',
      'Note: other agents may need clap wiring for new flags (`clone --force`, `gc --yes`). You own main.rs, so',
      'ADD any flags requested in their handoff notes — read the notes if provided, and if a flag is described in',
      'your instructions for another file, wire it here and pass it through.',
    ].join('\n'),
  },
  {
    key: 'service-errors',
    prompt: [
      'FILES YOU OWN: apps/cli/src/service.rs, apps/cli/src/logrotate.rs, apps/cli/src/progress.rs,',
      'apps/cli/src/errors.rs, apps/cli/src/auth.rs',
      '',
      'DEFECT 1 (MEDIUM, the installed daemon cannot authenticate). `install` captures a fixed list of env vars',
      'into service.env (service.rs:~62-67) and `CONVEX_SITE_URL` is missing, so the daemon cannot complete the',
      'Better Auth JWT exchange that every Coordinator call needs. Verify against apps/cli/src/auth.rs and',
      'apps/cli/src/env.rs (read-only for the env.rs part) exactly which variables the auth path reads, and capture',
      'the full set. Separately, `RUST_LOG` is in that captured list — another agent is fixing a secret-leak that',
      'makes a captured `RUST_LOG=debug` write JWTs into daemon.log forever, so REMOVE `RUST_LOG` from the',
      'captured set (their fix caps third-party log targets; not capturing it at all is the belt-and-braces half).',
      '',
      'DEFECT 2 (MEDIUM, credentials silently truncated). `service install` writes service.env in a way that',
      'silently truncates values (service.rs:~336) — anything with a newline, a quote, or an `=` is mangled, so a',
      'perfectly valid secret produces a daemon that fails to authenticate with no indication why.',
      'FIX: quote/escape values correctly for the format each platform expects (systemd EnvironmentFile and a',
      'launchd plist have DIFFERENT rules — check both), or refuse to write a value that cannot be represented,',
      'with a clear error. Also verify the file is written 0600: it contains the session token.',
      '',
      'DEFECT 3 (MEDIUM, the daemon dies at logout on Linux). `install` on systemd (~line 361) never enables',
      'lingering, so a `--user` service stops when the user logs out — for a VPS/headless box (a primary use case',
      'per docs/MAC-SETUP.md and the project\'s own Mac<->VPS pair) sync silently stops on every logout.',
      'FIX: enable lingering (`loginctl enable-linger`) during install, and if that fails (no permission), SAY SO',
      'with the exact command the user must run. Make install idempotent either way.',
      '',
      'DEFECT 4 (LOW, unbounded log). `daemon.err.log` is never rotated (service.rs:~58) while `daemon.log` is,',
      'and `service status` slurps the whole file into memory to show a tail.',
      'FIX: rotate both (logrotate.rs is yours — reuse it), and read only the tail rather than the whole file.',
      '',
      'DEFECT 5 (LOW, misleading progress). The progress line reports `total/total` after a phase FAILED',
      '(progress.rs:~97), so a failed sync ends by printing what looks like completion.',
      'FIX: distinguish completion from abandonment in the final render.',
      '',
      'DEFECT 6 (LOW/MEDIUM, error rendering). apps/cli/src/errors.rs is the human-facing error renderer, and:',
      '  (a) all of its messages are in SPANISH while the rest of the CLI, the README and `--help` are in ENGLISH.',
      '      Translate them to English, keeping the same structure (one-line message + a concrete next step).',
      '      Note crates/ft-conflict also writes a SPANISH marker into conflict-copy FILENAMES on the user\'s disk',
      '      and the recognizer is keyed to that exact literal — you do NOT own that file; report it as a handoff',
      '      note, and be careful: changing the literal would stop existing conflict copies from being recognized,',
      '      so it needs a compatibility path, not a blind rename.',
      '  (b) it tells the user to re-run with `RUST_LOG=debug` for detail — which another agent has established',
      '      leaks the Space key and the auth JWT into the log. Change the hint to `-v` (which is `info` and does',
      '      not leak).',
      '  (c) new typed errors now exist that deserve human phrasing: `EngineError::Refused` (a safety guard',
      '      declined a destructive operation), `EngineError::SpaceLocked` (another process holds the Space lock),',
      '      `ft_core::Error::UnsafePath` (a manifest entry tried to escape the Space root — this one is a',
      '      SECURITY event and should tell the user something is wrong with the Space/Coordinator, not just',
      '      print a path), and manifest/block integrity mismatches (`PageCidMismatch`, a reassembled file failing',
      '      its `pcid` check). Read the current error enums in crates/ft-engine/src/error.rs,',
      '      crates/ft-manifest/src/lib.rs, crates/ft-diff/src/lib.rs and crates/ft-core/src/lib.rs to see what',
      '      actually exists NOW (other agents just added variants), and give each a message + next step.',
      '  (d) a version-skew failure (an older binary meeting a newer manifest format) should say "run',
      '      `filething update`" rather than surfacing a raw discriminant error.',
      '',
      'DEFECT 7 (check and report). apps/cli/src/auth.rs is yours: verify the Better Auth HTTP flow does not put',
      'the password anywhere it could leak (argv, logs, error messages), that its reqwest client has timeouts (the',
      'audit says it does — confirm), and that a token expiring mid-operation produces a clear message rather than',
      'a raw 401. Fix what is broken; report what is already fine.',
    ].join('\n'),
  },
]

GROUPS.push({
  key: 'coordinator',
  prompt: [
    'FILES YOU OWN: crates/ft-coordinator/src/lib.rs',
    'Run tests with: cargo test -p ft-coordinator   (and clippy/fmt with -p ft-coordinator)',
    '',
    'This is the Rust client for the Convex Coordinator. The BACKEND has just been changed by another agent and',
    'this crate must catch up. Read packages/backend/convex/*.ts (read-only for you) to see the current server',
    'contract before you start.',
    '',
    'DEFECT 1 (MEDIUM, a fragile error classification that can cause an infinite retry). `classify_convex_error`',
    '(~line 821) / `is_conflict` (~line 854) decides a commit CAS conflict by testing whether the error MESSAGE',
    'CONTAINS the substring "conflict". That is dangerously loose: any unrelated server error whose text happens',
    'to contain that word makes the client treat a DETERMINISTIC failure as a retryable CAS race and retry it',
    'forever. The backend now returns typed `code` fields, so matching on the code is available and correct.',
    'FIX: classify on the structured `ConvexError.data.code` (the backend sets `code: "conflict"` explicitly for',
    'the real CAS case) and stop relying on substring matching. Keep a conservative fallback if a code is absent',
    'so an old deployment still works, and make sure the fallback cannot misfire on the new codes.',
    'New codes the backend can now return, which you should map to typed variants rather than a generic',
    '`Function(message)`: `too_many_revisions`, `bad_manifest_root_cid`, `bad_meta_blob_cid`, plus the existing',
    '`bad_space_key`, `space_key_already_set`, `storage_unconfigured`, `vault_unavailable`, `bad_key`,',
    '`bad_request`, `dangling_head`, `unauthenticated`, `no_account`, `forbidden`, `space_not_found`.',
    '',
    'DEFECT 2 (MEDIUM, GC breaks on a long history). `revisions:listFromSeq` used to `.collect()` the whole',
    'Revision chain. The server now refuses past 4096 revisions with `ConvexError { code: "too_many_revisions",',
    'limit, minSeq, maxSeq }` rather than silently truncating (a truncated mark set would make the GC delete live',
    'data). It also accepts a NEW OPTIONAL inclusive `maxSeq` argument for windowed paging.',
    'FIX: make `list_revisions_from` (the client wrapper) page: walk from `min_seq` to the head seq — which the',
    'caller already knows from `spaces:head` — in windows comfortably under the server bound, concatenating the',
    'results. The server arg is optional, so sending it is backwards-compatible with an older deployment. Be',
    'careful that the windows cannot silently skip or duplicate a revision, and that the concatenated result is',
    'still ordered by seq — the GC\'s correctness depends on the set being COMPLETE, so if anything is uncertain,',
    'fail loudly rather than returning a partial list. If the public signature has to change to accept the head',
    'seq, that is allowed here — but grep every caller (crates/ft-engine/src/gc.rs is one, and another agent may',
    'be editing it right now) and report exactly what you changed so the call sites can be updated.',
    '',
    'DEFECT 3 (HIGH, no timeouts — hangs forever). Verify how this crate handles an unreachable Coordinator.',
    '`ConvexClient::new` returns Ok before any socket is established, so callers hang indefinitely with the',
    'convex crate logging a raw ERROR line every ~15s. Another agent is adding a timeout around the CONNECT in',
    'apps/cli/src/env.rs; your half is to check whether individual query/mutation calls here can also hang',
    'unboundedly after a successful connect (a websocket that stops responding), and to bound them if so. Report',
    'what you find; do not duplicate the CLI-side timeout.',
    '',
    'DEFECT 4 (investigate). The backend now rate-limits sign-in/sign-up and returns HTTP 429. Check whether',
    'anything in this crate (or the auth exchange it performs) would treat a 429 as a generic failure and retry',
    'immediately, turning a rate limit into a hot loop. If the retry logic lives in apps/cli/src/auth.rs instead,',
    'report it as a handoff note rather than editing that file.',
    '',
    'Also: this crate is where `seq` and `baseSeqInUse` cross the wire as Float64 (a past bug, per TODO.md).',
    'Convex numbers are f64, so a `seq` beyond 2^53 would silently lose precision. Check whether any value that',
    'crosses this boundary could realistically exceed that, and if the answer is "not in any plausible lifetime",',
    'say so and move on rather than adding ceremony.',
  ].join('\n'),
})

// The ft-engine wave was INTERRUPTED mid-flight. The tree is green (609 tests,
// clippy+fmt clean) and the headline items landed, but no agent filed a report, so
// nobody knows which of their later assigned defects never got done. This agent
// reconstructs that from the diff. It is read-only apart from the gaps it closes.
GROUPS.push({
  key: 'engine-gaps',
  prompt: [
    'FILES YOU OWN: everything under crates/ft-engine/src/ and crates/ft-engine/tests/.',
    'Run tests with: cargo test -p ft-engine (plus clippy/fmt -p ft-engine).',
    '',
    'SITUATION: five agents were fixing ft-engine in parallel and were ALL INTERRUPTED before filing a report.',
    'The tree happens to be green (`cargo test --workspace` = 609 passed / 0 failed, clippy and fmt clean) and',
    'spot checks show the headline fixes landed. But because no report exists, nobody knows which of the LATER',
    'defects on each agent\'s list never got implemented. Your job is to find and close those gaps.',
    '',
    'METHOD: run `git diff -- crates/ft-engine` to see everything that changed, then go defect by defect through',
    'the list below and determine for EACH one: implemented (with a test?), partially implemented, or missing.',
    'Verify by reading the code — not by assuming a plausible-looking diff covers it. Where a defect is missing',
    'or partial, IMPLEMENT it to the same standard (fix + a test that fails before and passes after).',
    'Where it is already done, say so and move on — do NOT redo or "improve" completed work, and do not',
    'refactor. Prefer leaving a correct implementation alone over making it prettier.',
    '',
    'THE FULL ASSIGNED LIST (5 agents, all files now yours):',
    '',
    'gc.rs — (1) refuse when `list_mine()` is empty; (2) refuse unless the account owns the Space the CLI',
    'pointed at; (3) a max-sweep-fraction proportion guard, overridable via GcOptions; (4) re-read heads after',
    'the sweep and document the residual commit/GC race; (5) conservative clock-skew handling on the grace',
    'window; (6) keep and report the deleted set when a delete fails partway.',
    '',
    'scan.rs — (1) skip unreadable/non-UTF8 entries instead of aborting the whole scan, KEEPING their previous',
    'index row and Manifest entry so a transient EACCES never publishes a deletion, and report skipped paths;',
    '(2) never open FIFOs/sockets/device nodes (a FIFO blocks `fs::read` forever); (3) detect casefold-key',
    'collisions and never hand duplicate keys to `ft_manifest::build`; (4) stop Derived (t=2) paths',
    'round-tripping as deletions and wedging the origin device forever; (5) the §9 (size, mtime) fast path;',
    '(6) bound the `blocks_to_upload` buffer so a large Space cannot OOM; (7) `.filethingignore` glob support or',
    'a loud warning for unsupported patterns, and adding an ignore entry must NOT publish deletions of the',
    'newly-ignored files; (8) exclude `ft_diff::TMP_SUFFIX` scratch files from the scan.',
    '',
    'pull.rs — (1) a conflict copy must never be overwritten by a phase-B winner, nor by a repeat conflict at',
    'the same path; (2) a local exec-bit change must not be silently dropped by the auto-merge; (3) a',
    'casefold/NFC collision must not mint a new conflict copy every sync round; (4) a dir->file replacement over',
    'a dir holding unsynced content must fail ACTIONABLY rather than wedging pull forever; (5) propagate (never',
    'swallow) a failure to persist the sync base; (6) `validate_untrusted` at this file\'s own trust boundary;',
    '(7) build the empty Manifest root locally instead of HEAD-then-PUT into the Vault; (8) adopt ft-index\'s',
    'batch writers where correct (never batching rows for files not yet written to disk).',
    '',
    'context.rs / secrets.rs — (1) `decode_page_verified` in `read_manifest_entries`; (2) `validate_untrusted`',
    'on every entry admitted there; (3) do not silently collapse casefold-colliding entries in the',
    'HashMap<CasefoldKey, FileEntry>; (4) verify the meta blob against its `metaBlobCid` before installing the',
    'chunk secret; (5) a per-Space advisory lock released by the OS on death; (6) turn a newer-format decode',
    'failure into an error that tells the user to run `filething update`.',
    '',
    'run.rs / commit.rs / clone.rs — (1) `has_mark` short-circuit so echo suppression stops reading+hashing',
    'whole files on every event (and stream/spawn_blocking the hash for the marked case); (2) wire ft-watcher\'s',
    '`is_healthy()`/`rewatch()` into the watchdog tick AND force a full scan after a successful re-arm, since',
    'the blind window produced no events; (3) a proportion guard so an empty/half-mounted root cannot commit a',
    'wholesale deletion; (4) move whole-tree blocking FS work off the async task where the types allow;',
    '(5) ignore TMP_SUFFIX watcher events so every pull is not followed by a redundant commit; (6) make sure a',
    'permanently-failing Space is quarantined and VISIBLE rather than retrying identically forever.',
    '',
    'Report per defect: DONE (and how you verified) / PARTIAL (what was missing, what you added) / MISSING (what',
    'you implemented). Be honest about anything you chose to leave — an accurate gap list is worth more than a',
    'claim of completeness.',
  ].join('\n'),
})

phase('Fix')
log('Wave 3: ' + GROUPS.length + ' agents (apps/cli + ft-coordinator + ft-engine gap closure)')

const results = await parallel(
  GROUPS.map((g) => () =>
    agent(SHARED + '\n\n=== YOUR ASSIGNMENT ===\n' + g.prompt, {
      label: 'fix:' + g.key,
      phase: 'Fix',
      effort: 'xhigh',
    }).then((report) => ({ group: g.key, report })),
  ),
)

return results.filter(Boolean)
