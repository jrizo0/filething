# Rust
/target/
**/target/
Cargo.lock.orig

# Node / Bun / Turborepo
node_modules/
.turbo/
dist/
*.tsbuildinfo
.convex/

# Local data
*.db
*.db-shm
*.db-wal
.filething/

# Env / secrets
.env
.env.local
.env.*.local
# Managed-infra credentials (Convex Cloud + R2). The *.example templates ARE
# tracked; the filled-in .env.cloud is a secret and must never be committed.
.env.cloud

# OS / editor cruft
.DS_Store
*.swp
*~
