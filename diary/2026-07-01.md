# Diario — filething — 2026-07-01

Sesión de diagnóstico y fixes de los hallazgos de la prueba manual Mac↔VPS del 2026-06-25
(ver `diary/2026-06-25.md`). Todo validado end-to-end; **sin commitear** (pendiente de ok).

## Diagnóstico (3 vías independientes)

**1. Forense en Convex** — se reconstruyó el timeline completo de las 13 Revisions (seq 0–12)
del Space `demo`, diffeando los manifests desde MinIO. Conclusión central: **los commits nunca
fallaron**. La modificación "que no llegó" (C2) sí se commiteó (seq 8, VPS, 20:51:36) y el
borrado "que no llegó" (C4) también (seq 9, VPS, 20:57:41, justo al inicio del corte). La Mac
siguió commiteando durante la ventana (seq 10–12, 20:59) pero **nunca aplicó** seq 8 ni 9:
tenía el camino de commits vivo y el de recepción muerto.

**2. Reproducción local** (2 devices simulados, red estable):
- C2 **reproduce** y no es artefacto: la 1ª edición de un archivo sincroniza; la 2ª y todas
  las siguientes se pierden en silencio para siempre (daemon vivo, path-specific).
- C4 **no reproduce**: borrados propagan en ~2 s en ambas direcciones, sin importar qué
  device originó el archivo. Fue artefacto del corte + huecos de resiliencia.
- Latencia (C1): sin asimetría real entre crear y borrar; la demora observada era del lado
  que aplica (feed dormido).

**3. Lectura del código** — tres defectos con línea exacta (los tres arreglados hoy):

| # | Defecto | Dónde | Efecto |
|---|---|---|---|
| 1 | Buffer de coalescing sin expiración (solo `insert`, nunca purga) | `ft-watcher/src/lib.rs` | 2ª modificación del mismo `(kind, path)` jamás se reenvía en la vida del daemon |
| 2 | `run()` hacía pull inicial pero ningún commit inicial | `ft-engine/src/run.rs` | cambios locales hechos con el daemon apagado quedaban sin commitear indefinidamente |
| 3 | Cero red de seguridad si el feed muere en silencio (túnel/VPN inestable) | `ft-engine/src/run.rs` | daemon vivo pero sordo: commitea y no aplica — exactamente lo observado en la Mac |

## Fixes (con TDD)

1. **`CoalesceBuffer` con ventana real de 50 ms** (`ft-watcher`): reenvía si la key es nueva o
   pasó la ventana; la supresión NO extiende la ventana (una ráfaga continua no mata de hambre
   al canal); purga oportunista. Invariante documentado: ventana ≤ `COMMIT_DEBOUNCE` (300 ms)
   del engine. 6 tests nuevos; el test del bug falla contra el código viejo (verificado).
2. **`startup_sync()`** (`run.rs`): al montar, pull inicial + commit inicial. El commit es
   no-op barato si no hay nada (corta en `commit.rs:94-96` antes de tocar Vault/Coordinator).
   3 tests nuevos (`tests/watch_resilience.rs`).
3. **Pull de respaldo cada 30 s** (`FALLBACK_PULL_INTERVAL`, `run.rs`): recupera un feed
   muerto sin reiniciar y acota la latencia de aplicar a ~30 s en el peor caso. Sus errores
   se loguean y se reintenta al siguiente tick (el timer corre también en pleno corte; matar
   al daemon por un fallo transitorio anularía el propósito del backstop).
4. **Exclusión built-in de basura de plataforma** (`scan.rs`, ADR 0011): `.DS_Store`,
   `Thumbs.db`, `desktop.ini` (nombre exacto) nunca entran al Manifest; un Space que ya los
   tenía se auto-limpia en el siguiente commit. 4 tests nuevos (`tests/junk_ignore.rs`).
   Motivado por el `.DS_Store` de la Mac que viajó al VPS el día 25.

## Extras

- `scripts/demo-gates.sh`: **2 gates nuevos** — (e) segunda edición del mismo archivo vía
  daemons (falla con el bug viejo del coalesce), (f) arranque offline (borrar + crear con el
  daemon apagado → al arrancar propaga sin tocar nada más). Además: cleanup de daemons por
  PID con `trap EXIT`, y fix de un flake real preexistente (la extracción del pairing code
  podía capturar dígitos del account id; ahora está anclada a `--code`).
- `docs/MAC-SETUP.md`: túnel SSH con auto-reconexión, daemon detached con log en append
  (`>>`), advertencia de daemon único (el 25 quedaron dos vivos).
- `TODO.md`: hallazgos marcados como resueltos + **Roadmap a producción** (Fase A: Convex
  Cloud + R2; Fase B: auth real, cifrado, servicio, GC, billing).
- Fix previo del día (ya commiteado en `464496c`): persistir `last_synced_revision_id` para
  el falso "behind" de `status`.

## Validación

- `cargo test --workspace`: **283 pasan, 0 fallan** (incluye los 13 tests nuevos).
- Los 4 tests live (`--ignored`) contra Convex+MinIO reales: **los 4 pasan**.
- `clippy -D warnings` y `fmt --check`: limpios.
- `scripts/demo-gates.sh` completo, gates **a–f**: **los 6 pasan** contra infra viva.
- Check puntual del junk: `.DS_Store` creado con daemon vivo NO viaja ni entra al manifest;
  un archivo normal creado justo después llega en ~1 s.

## Pendientes al cerrar

- [ ] Commit de todo esto (cuando el usuario lo pida).
- [ ] Re-correr la prueba manual Mac↔VPS con los fixes (`docs/MAC-SETUP.md`).
- [ ] Arreglar el healthcheck de Convex en `infra/docker-compose.yml` (contenedor "unhealthy"
  pero el servicio responde 200; el comando de chequeo falla por timeout).
- [ ] Roadmap a producción: ver `TODO.md`.
